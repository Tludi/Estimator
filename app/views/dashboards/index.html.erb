<% title "Dashboards" %>

<table>
  <tr>
    <th>Name</th>
    <th>User</th>
  </tr>
  <% for dashboard in @dashboards %>
    <tr>
      <td><%= dashboard.name %></td>
      <td><%= dashboard.user_id %></td>
      <td><%= link_to "Show", dashboard %></td>
      <td><%= link_to "Edit", edit_dashboard_path(dashboard) %></td>
      <td><%= link_to "Destroy", dashboard, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Dashboard", new_dashboard_path %></p>
